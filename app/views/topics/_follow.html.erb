<%= form_with(model: current_user.user_topic_relations.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :topic_id, @topic.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>