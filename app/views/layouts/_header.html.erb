<nav class="navbar navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">
      <img src="<%= image_path("logo.jpg") %>" alt="" width="30" height="30">
      stack<strong>underflow</strong>
    </a>
    <button height="50" class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <img src="<%= image_path("arrow-down-square.svg") %>" height="30" width="30">
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <%= form_tag(search_url, :method => "get") do %> 
          <div class="form-input input-group">
            <%= search_field_tag :search, params[:search], placeholder: "Search question", class: "form-control" %>
            <div class="input-group-append">
              <%= button_tag "Search", :class => 'btn btn-outline-warning',:name => nil%> 
            </div>
          </div>
        <% end %>
        <li class="nav-item active">
          <%= link_to root_path, class: "nav-link" do %>
            <span class="sr-only">Home</span>
          <% end %>
        </li>
        
        <% if logged_in? %>
          <li class="dropdown">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Questions
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                <li><a class="dropdown-item" href="<%= questions_path %>">All questions</a></li>
                <li><a class="dropdown-item" href="<%= questions_user_path(current_user.id) %>">Your questions</a></li>
                <li><a class="dropdown-item" href="<%= answered_questions_user_path(@current_user.id) %>">Your answers</a></li>
                <li><a class="dropdown-item" href="<%= voted_questions_user_path(@current_user.id) %>">Voted answers</a></li>
              </ul>
            </li>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Questions", questions_path, class: "nav-link" %>
          </li>
        <% end %>
        
        <li class="nav-item">
          <%= link_to "Topics", topics_path, class: "nav-link" %>
        </li>
        <% if logged_in? %>
          <li class="nav-item">
            <%#= link_to "Users", users_path, class: "nav-link" %>
          </li>
          
          <li class="dropdown">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Account
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                <li><a class="dropdown-item" href="<%= user_path(current_user) %>">Profile</a></li>
                <li><a class="dropdown-item" href="<%= edit_user_path(current_user) %>">Settings</a></li>
                <li><%= link_to "Help", help_path, class: "dropdown-item"%></li>
                <li>
                  <%= link_to "Log out", logout_path, method: :delete, class: "dropdown-item" %>
                </li>
              </ul>
            </li>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path, class: "nav-link" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>