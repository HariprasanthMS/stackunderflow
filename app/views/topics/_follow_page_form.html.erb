<div id="follow_form">
  <% if current_user.following?(topic) %>
    <%= render partial: 'unfollow', locals: { topic: topic } %>
  <% else %>
    <%= render partial: 'follow', locals: { topic: topic } %>
  <% end %>
</div>