<div class="row">
  <%= render 'stats' %>

  <div class="col-md-8">

    <% if @my_answered_questions.blank? %>
      <div class="empty-questions">
        You currently haven't answered any quesiton.. 
      </div>
    <% else %>
      <div class="suggested-questions">
        <h3><%= "Question".pluralize(@my_answered_questions.count) %> you answered</h3>
        <ul class="list-group">
          <% @my_answered_questions.each do | question | %>
            <%= render partial: 'shared/item', locals: { question: question, answer: @answer } %>
          <% end %>
        </ul>
      </div>
    <% end %>
  </div>
</div>