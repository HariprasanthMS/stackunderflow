<%= form_with(model: current_user.user_topic_relations.build, remote: true, class: "follow-form") do |f| %>
  <div><%= hidden_field_tag :topic_id, topic.id %></div>
  <%= f.submit "Follow", class: "btn btn-outline-warning bd-10 w-100" %>
<% end %>