<% provide(:title, "Edit user") %>
<% provide(:button_text, 'Save changes') %>

<div class="center row justify-content-center signup">
  <div class="col-6">
    <h1>Update your profile</h1>
    <div class="gravatar_edit text-end">
      <%= gravatar_for @user %>
    </div>
    <%= render 'form' %>
    <% if false %>
    # <div class="gravatar_edit text-end">
    #   <%= gravatar_for @user %>
    #   <%# a href="https://gravatar.com/emails" target="_blank", rel="noopener" style="text-decoration: none; color: greenyellow;">change</a%>
    # </div>
    <% end %>
  </div>
</div>