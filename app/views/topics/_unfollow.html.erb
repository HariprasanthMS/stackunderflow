<%= form_with(model: current_user.user_topic_relations.find_by(topic_id: topic.id),
             html: { method: :delete }, remote: true, class: "follow-form") do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-outline-warning bd-10 w-100" %>
<% end %>